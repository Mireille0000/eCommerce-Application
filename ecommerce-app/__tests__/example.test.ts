describe('addition tests', () => {
    test('adding 2 and 2 gives 4', () => {
        const result = 2 + 2;
        expect(result).toBe(4);
    });
    test('adding 3 and 4 gives 7', () => {
        const result = 3 + 4;
        expect(result).toBe(7);
    });
    // ... и другие тесты для сложения
});
