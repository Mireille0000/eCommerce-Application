export const errMsgsWord = [
  'The field must contain at least one character',
  'Field must not contain special characters or digits',
];

export const errMsgsBirthDate = ['Invalid input', 'You must be at least 13 years old'];

export const errorMsgsEmail = [
  'Email should be in a proper format (e.g. user@example.com, to-user@example.fr etc.)',
  'Email should contain an arobase (@)',
  'Email should not contain any leading or trailing whitespaces',
];

export const errorMsgsPassword = [
  'Password must be at least 8 characters long',
  'Password must contain at least one uppercase letter (A-Z)',
  'Password must contain at least one lowercase letter (a-z)',
  'Password must contain at least one digit (0-9)',
  'Password must not contain leading or trailing whitespace',
  'Password must contain at least one special character (e.g., !@#$%^&*)',
];

export const errMsgsStreet = ['The field must contain at least one character'];

export const errMsgsHouseNumber = ['The value must start with a number', 'The value must not have special characters'];

export const errMsgsPostcode = ['Postcode length must be 5 characters', 'Postal code must consist of numbers only'];

export const errEmailPersonal = ['Your email is incorrectly formatted'];

export const errBirthPersonal = ['Invalid input (YYYY-MM-DD)', 'You must be at least 13 years old'];
